<UserControl x:Class="MailClient.View.MailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MailClient.View"
             xmlns:dictionary="clr-namespace:MailClient.View.Dictionary"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">

    <StackPanel>
        <TextBlock Style="{StaticResource PageName}" Text="{Binding PageName}"/>
        <StackPanel Style="{StaticResource StackPanel}">
            <TextBlock Text="{Binding Source={x:Static dictionary:ViewTexts.From}, Mode=OneWay}" Style="{StaticResource LabelTextBlock}"/>
            <TextBox Text="{Binding Mail.From}" Style="{StaticResource MailTextBox}"/>
            <TextBlock Text="{Binding Source={x:Static dictionary:ViewTexts.To}, Mode=OneWay}" Style="{StaticResource LabelTextBlock}"/>
            <TextBox Text="{Binding Mail.To}" Style="{StaticResource MailTextBox}"/>
            <TextBlock Text="{Binding Source={x:Static dictionary:ViewTexts.Subject}, Mode=OneWay}" Style="{StaticResource LabelTextBlock}"/>
            <TextBox Text="{Binding Mail.Subject}" Style="{StaticResource MailTextBox}"/>
            <TextBlock Text="{Binding Source={x:Static dictionary:ViewTexts.Message}, Mode=OneWay}" Style="{StaticResource LabelTextBlock}"/>
            <TextBox                 
                    Text="{Binding Mail.Message}"
                    Style="{StaticResource MessageTextBox}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
