<UserControl x:Class="MailClient.View.LoggingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MailClient.View"
      xmlns:viewmodel="clr-namespace:MailClient.ViewModel" 
      xmlns:viewstyle="clr-namespace:MailClient.ViewStyle"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="600">

    <UserControl.DataContext>
        <viewmodel:LoggingViewModel/>
    </UserControl.DataContext> 
    <UserControl.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ViewStyle/LoggingViewStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel viewstyle:MarginSetterViewStyle.Margin="0,3">
        <TextBlock Style="{StaticResource TextBlock}" IsEnabled="False" Height="Auto" Width="Auto" TextAlignment="Center" TextWrapping="WrapWithOverflow">Please insert your email login, password and choose email client type</TextBlock>
        <Label Style="{StaticResource Label}">_Login</Label>
        <TextBox Name="boxLogin" Text="{Binding Path=Login}" Style="{StaticResource Login}" Grid.Row="1" Grid.Column="1"/>
        <Label Style="{StaticResource Label}">_Password</Label>
        <PasswordBox Name="boxPassword" FontSize="24" Width="300" Height="40" PasswordChanged="boxPassword_PasswordChanged" Grid.Row="2" Grid.Column="1"/>
        <Label Style="{StaticResource Label}">Email type</Label>
        <RadioButton Style="{StaticResource RadioButton}" Content="Gmail" GroupName="Mode" IsChecked="{Binding Path=EmailModeTable[0], Mode=TwoWay}"/>
        <RadioButton Style="{StaticResource RadioButton}" Content="WP" GroupName="Mode" IsChecked="{Binding Path=EmailModeTable[1], Mode=TwoWay}"/>
        <RadioButton Style="{StaticResource RadioButton}" Content="Outlook" GroupName="Mode" IsChecked="{Binding Path=EmailModeTable[2], Mode=TwoWay}"/>
        <Button Content="Log in" Style="{StaticResource LoginButton}" Command="{Binding LogInCommand}"/>
    </StackPanel>

</UserControl>
